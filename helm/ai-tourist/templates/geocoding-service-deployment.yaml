apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.geocodingService.name }}
  labels:
    app: {{ .Values.geocodingService.name }}
    {{- include "ai-tourist.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.geocodingService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.geocodingService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.geocodingService.name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.geocodingService.metricsPort }}"
    spec:
      containers:
      - name: geocoding-service
        image: "{{ .Values.geocodingService.image }}:{{ .Values.geocodingService.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - name: grpc
          containerPort: {{ .Values.geocodingService.port }}
        - name: metrics
          containerPort: {{ .Values.geocodingService.metricsPort }}
        envFrom:
        - configMapRef:
            name: {{ include "ai-tourist.fullname" . }}-config
        - secretRef:
            name: {{ include "ai-tourist.fullname" . }}-secrets
        resources:
          {{- toYaml .Values.geocodingService.resources | nindent 12 }}
        livenessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.geocodingService.name }}
  labels:
    app: {{ .Values.geocodingService.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.geocodingService.port }}
    targetPort: grpc
    protocol: TCP
    name: grpc
  - port: {{ .Values.geocodingService.metricsPort }}
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app: {{ .Values.geocodingService.name }}
