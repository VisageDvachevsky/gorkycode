apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.routePlannerService.name }}
  labels:
    app: {{ .Values.routePlannerService.name }}
    {{- include "ai-tourist.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.routePlannerService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.routePlannerService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.routePlannerService.name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.routePlannerService.metricsPort }}"
    spec:
      containers:
      - name: route-planner-service
        image: "{{ .Values.routePlannerService.image }}:{{ .Values.routePlannerService.tag }}"
        imagePullPolicy: IfNotPresent
        ports:
        - name: grpc
          containerPort: {{ .Values.routePlannerService.port }}
        - name: metrics
          containerPort: {{ .Values.routePlannerService.metricsPort }}
        envFrom:
        - configMapRef:
            name: {{ include "ai-tourist.fullname" . }}-config
        - secretRef:
            name: {{ include "ai-tourist.fullname" . }}-secrets
        resources:
          {{- toYaml .Values.routePlannerService.resources | nindent 12 }}
        livenessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.routePlannerService.name }}
  labels:
    app: {{ .Values.routePlannerService.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.routePlannerService.port }}
    targetPort: grpc
    protocol: TCP
    name: grpc
  - port: {{ .Values.routePlannerService.metricsPort }}
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app: {{ .Values.routePlannerService.name }}
