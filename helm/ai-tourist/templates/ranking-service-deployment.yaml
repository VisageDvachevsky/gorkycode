apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.rankingService.name }}
  labels:
    app: {{ .Values.rankingService.name }}
    {{- include "ai-tourist.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.rankingService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.rankingService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.rankingService.name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.rankingService.metricsPort }}"
    spec:
      containers:
      - name: ranking-service
        image: "{{ .Values.rankingService.image }}:{{ .Values.rankingService.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - name: grpc
          containerPort: {{ .Values.rankingService.port }}
        - name: metrics
          containerPort: {{ .Values.rankingService.metricsPort }}
        envFrom:
        - configMapRef:
            name: {{ include "ai-tourist.fullname" . }}-config
        - secretRef:
            name: {{ include "ai-tourist.fullname" . }}-secrets
        resources:
          {{- toYaml .Values.rankingService.resources | nindent 12 }}
        livenessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.rankingService.name }}
  labels:
    app: {{ .Values.rankingService.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.rankingService.port }}
    targetPort: grpc
    protocol: TCP
    name: grpc
  - port: {{ .Values.rankingService.metricsPort }}
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app: {{ .Values.rankingService.name }}
