apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.llmService.name }}
  labels:
    app: {{ .Values.llmService.name }}
    {{- include "ai-tourist.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.llmService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.llmService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.llmService.name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.llmService.metricsPort }}"
    spec:
      containers:
      - name: llm-service
        image: "{{ .Values.llmService.image }}:{{ .Values.llmService.tag }}"
        imagePullPolicy: IfNotPresent
        ports:
        - name: grpc
          containerPort: {{ .Values.llmService.port }}
        - name: metrics
          containerPort: {{ .Values.llmService.metricsPort }}
        envFrom:
        - configMapRef:
            name: {{ include "ai-tourist.fullname" . }}-config
        - secretRef:
            name: {{ include "ai-tourist.fullname" . }}-secrets
        resources:
          {{- toYaml .Values.llmService.resources | nindent 12 }}
        livenessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: grpc
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.llmService.name }}
  labels:
    app: {{ .Values.llmService.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.llmService.port }}
    targetPort: grpc
    protocol: TCP
    name: grpc
  - port: {{ .Values.llmService.metricsPort }}
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app: {{ .Values.llmService.name }}
